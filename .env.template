# Dremio API Credentials Template (For local n8n testing)

# To use these credentials when running a local n8n instance or tests,
# populate these variables based on your Dremio Cloud or Software instance.

# Type of Dremio instance (cloud | software)
DREMIO_TYPE=cloud

# The API base URL. 
# For Cloud: https://api.dremio.cloud
# For Software: https://<HOST>:9047/api/v3
DREMIO_BASE_URL=https://api.dremio.cloud

# The Project ID (Required if Type is 'cloud')
DREMIO_PROJECT_ID=your_dremio_project_id_here

# Personal Access Token
DREMIO_TOKEN=your_personal_access_token_here

# Whether to ignore SSL certificate validation (useful for self-signed certs with Software edition)
# DREMIO_IGNORE_SSL=true
